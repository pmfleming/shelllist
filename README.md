# shelllist

Quickshell experiments and UI components for desktop menus.

Initial goal: build a Hyprland-friendly Wi-Fi/network popup backed by the existing `nm-wifi-rofi` NetworkManager D-Bus helper.
